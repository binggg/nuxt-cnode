{
  "envId": "{{env.ENV_ID}}",
  "version": "2.0",
  "framework": {
    "name": "nuxt-cnode",
    "plugins": {
      "nuxtjs-ssr": {
        "use": "@cloudbase/framework-plugin-nuxt",
        "inputs": {
          "buildCommand": "npm run build",
          "path": "/"
        }
      }
    }
  }
}
